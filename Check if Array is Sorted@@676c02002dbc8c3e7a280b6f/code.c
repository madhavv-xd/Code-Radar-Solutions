#include <stdio.h>

int main() {
   int a[];
   int i , number;
   scanf("%d" , &number)
   for(int i = 0; i < number; i++){
        scanf("%1d" , &a[i])
   }
   int n = a.size();
   for(int i = 0; i < n; i++){
    if(arr[i] > arr[i-1]){
        printf("%s", Sorted);
    }
    else{
        printf("%s", Not Sorted);
    }
   }
    return 0;
}